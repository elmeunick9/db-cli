<?xml version="1.0" encoding="UTF-8"?>
<catalog xmlns="urn:oasis:names:tc:entity:xmlns:xml:catalog">
{{#each schemas}}
{{#each tables}}
  <uri name="urn:db:{{../schema}}:{{name}}" uri="{{../schema}}/{{name}}.xsd" />
{{/each}}
{{/each}}

{{#each users}}
  <!--db:auth:user:{{id}} email="{{email}}" -->
{{/each}}
</catalog>